OPENAI_API_KEY=sk-your-server-side-key
OPENAI_MODEL=gpt-4.1-mini
VITE_OPENAI_MODEL=gpt-4.1-mini

# 本地如果使用 ClashX 且服务端请求 OpenAI 超时，可打开下面这一行。
# Vercel 线上部署通常不需要设置代理。
# HTTPS_PROXY=http://127.0.0.1:7890
